#include <stdio.h>
#include <limits.h> // INT_MAX / UINT_MAX / USHRT_MAX
#include <stdint.h>
#include <stdlib.h>


int main(void){
  /*
   * 0 é falso e qq coisa diferente é verdadeiro
   *  == (igual)
   *  != diferente
   *  && (and) 
   *  || (or)
   *  > maior q
   *  < menor q
   *  >= maior igual
   *  <= menor igual
   *  */
  return 0;
}
